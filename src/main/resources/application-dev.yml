spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kp
    username: root
    password: pwd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true

web:
  origin: "http://localhost:3000"
